

import 'package:flutter/material.dart';

class eee extends StatelessWidget {
  const eee({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('dergergergerg'),),
    );
  }
}

